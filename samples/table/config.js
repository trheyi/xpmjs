// XpmJS 配置文件
// 每个人环境不同，所以请不要提交此文件

var host = 'wss.xpmjs.com';
var option = {
    'app':2,
    'host':host,
    'https':host,
    'wss': host + '/ws-server',
    'table.prefix': 'demo',
    'user.table':'user'
}
module.exports = option;